/* 
-Students marks greater than and equal to 50 is pass
otherwise fail
*/
function studentMarks() {
    let marks = 49;
    if (marks >= 50) {
        alert("PASS");
    } else {
        alert("FAIL")
    }
}
